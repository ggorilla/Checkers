function setup() {
  createCanvas(451,451)
}

function draw() {
  background(0)
  fill(250  )

  var x = 50
  var y = 50
  

  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
  
  x = 50
  y += 50
  for (var i = 0; i < 8; i++) {
    rect(x, y, 50, 50)
    x += 50
  }
}